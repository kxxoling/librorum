language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"

services:
  - redis-server

# command to install dependencies
install:
  - pip install -r requirements.txt

# command to run tests
script: python librorum/testing.py
